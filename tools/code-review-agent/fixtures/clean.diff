diff --git a/src/pages/api/sessions.ts b/src/pages/api/sessions.ts
new file mode 100644
index 0000000..a1b2c3d
--- /dev/null
+++ b/src/pages/api/sessions.ts
@@ -0,0 +1,34 @@
+import type { APIRoute } from "astro";
+import { z } from "zod";
+import { getSupabase } from "@/lib/supabase";
+
+const createSessionSchema = z.object({
+  title: z.string().min(1).max(200),
+  score: z.number().int().min(0).max(100),
+});
+
+export const GET: APIRoute = async ({ locals }) => {
+  const user = locals.user;
+  if (!user) return new Response("Unauthorized", { status: 401 });
+
+  // RLS scopes rows to the authenticated user; we never trust a client userId.
+  const { data, error } = await getSupabase(locals).from("sessions").select("*");
+  if (error) return new Response("Failed to load sessions", { status: 500 });
+
+  return new Response(JSON.stringify(data), {
+    headers: { "content-type": "application/json" },
+  });
+};
+
+export const POST: APIRoute = async ({ request, locals }) => {
+  const user = locals.user;
+  if (!user) return new Response("Unauthorized", { status: 401 });
+
+  const parsed = createSessionSchema.safeParse(await request.json());
+  if (!parsed.success) return new Response("Invalid body", { status: 400 });
+
+  const { error } = await getSupabase(locals)
+    .from("sessions")
+    .insert({ user_id: user.id, ...parsed.data });
+  if (error) return new Response("Failed to create session", { status: 500 });
+
+  return new Response(JSON.stringify({ ok: true }), { status: 201 });
+};
diff --git a/src/pages/api/sessions.test.ts b/src/pages/api/sessions.test.ts
new file mode 100644
index 0000000..d4e5f6a
--- /dev/null
+++ b/src/pages/api/sessions.test.ts
@@ -0,0 +1,12 @@
+import { describe, it, expect } from "vitest";
+import { POST } from "./sessions";
+
+describe("POST /api/sessions", () => {
+  it("rejects an unauthenticated request with 401", async () => {
+    const req = new Request("http://localhost/api/sessions", { method: "POST" });
+    const res = await POST({ request: req, locals: {} } as never);
+    expect(res.status).toBe(401);
+  });
+});
